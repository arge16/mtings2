spring:
  devtools:
    restart:
      aditional-paths: src/main/java
  application:
    name: backend-cuotas-service
  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: update
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    plataform: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
    dbname: topeducationcuotas
    url: jdbc:mysql://localhost:3306/topeducationcuotas
    username: root
    password: argenis
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB